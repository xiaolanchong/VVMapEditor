VVMInterface DLL 

[*] Fixes
[+] Features
[-] Known bugs

2004-10-06 3.3.5
[+] добавлена GetAlignMap - возвращает список sp-точек, которыми задана координатная сетка
[+] добавлен memory leak detector

2004-10-07 3.3.6
[+] все сообщения в ресурсах, полный уникод, при передачи сообщения конвертация 

2004-10-08 3.3.7
[*] баг при повторных загрузках различных файла(кол-во рефпоинтов оставалось от предыдущего)

2004-10-10 3.4.0
[+] теперь сжимаем данный c помощью zlib
    в идеале хотелось бы полностью иерархическое хранилище с бмп, текстовыми файлами и тд
    
2004-10-11 3.4.1
[+] теперь один проект с длл VVMapInterface вместо VVMapInterface /lib/ & Dll /dll/

2004-10-08 3.4.2
[*] VVInterface options не сохраняется points per function - исправлено (АГ)
[+] GetErrorStringW возвращает ошибку в уникодной строке

2004-10-08 3.4.3
[+] GetHorizon (рисуется горизонт, линия с L==0)

2004-10-08 3.5.0
[+] проективное преобразование разделено на 2 части: видимую (L>0) и невидимую (L<0)..
	CVVMReal::CorrectLSign
[*] исправлена "бабочка": теперь на битовую карту видимости выводим только точки с L>0
	CVVMReal::CalculateVisibility
[*] CVVMReal::_FillRect - устранена "кусочность" зон
[*] CVVMReal::GetPoint - устранена проблема с индексом
[*] CVVMReal::GetXY - если L<0 координаты не усредняются => правильная форма и положение зон

2004-10-08 3.5.1
[*] баг в CVVMOptions::Serialize
[+] Align3D - AlignMap с учетом SP.Z
[*] выключил GetTrQ_Int - теперь оценивается только качество преобразования 
	(но не степень "похожести" преобразованных картинок)

19.10.2004

[*] Reset_All(void) Save_Delete_All(void) - вставил в начало CVVMReal::Laod => 

            возможна повторная загрузка файла

 

эксперименты с различными преобразованиями.

требования:

            1) Попадания в точки

2) Непрерывность

3) При движении от точки к точке плавный переход (то есть отрезки примерно в отрезки).

 

[+] CVVMReal::CheckInterpolation

[+] CVVMReal::GetXY_WM - алгоритм весовых расстояний

[+] CVVOptions::LoadSave, 15-ая версия vme - можно добавлять поля к Options, не опасаясь за совместимость, Serialize также пока работает

            неплохо бы автоматизировать вставку опций

 

20.10.2004

v 3 , 5 , 5 

[+] CVVMReal::GetXY_internal - для компиляции (не входит в крит-секцию)

            CVVMReal::GetXY - для внешнего пользовнаия - если не входит в крит-секцию - возвращает ошибку

[*] CVVMReal::ErrString + resource.h (намудрил)

 

[+] adjustment-алгоритм (алгоритм подгонки точки) - CVVMReal::AdjXY, включил в опции; CVVMReal::GetXY вызывает CVVMReal::GetXY_Adj

            CVVMOptions::ControlsToOpts(), CVVMOptions::OptsToControls(), CVVMOptions::OnCommand() (добавил код)

            dialog

 

21.10.2004

Опции.

 

22.10.2004

[+] критические ошибки < 0

в VVMInterface перед Крит ошобкой прсто меняем знак.. число по модулю пусть остается тем же.. это нужно для совместимостью со таблицей строк в ресурсе

            * изменения в CVVMReal::ErrString

 

[+] нейросеть (эксперимент):

 

            вход  = (x,y) на одном мастере

            выход = (x,y) на другом мастере

            работает не очень... непрерывностью даже и не пахнет..

            хотя некоторые фрагменты некоторых мастеров отображаются удовлетворительно

//функции

            данные CTransition::NeuroL_R & NeuroR_L
            CTransition::NeuroTrans
            CVVMReal::NeuroCompile (вызывается из CVVMReal::CompileTranse)
            * время компиляции значит. увеличилось
            CVVMReal::GetXY_Neural
            + добавил опций
            [-] Neuro-преобразования не сохраняются в файл.. т.к. не слишком хорошо работают

 

[+] СММVreal::GetXY_spy (эксперимент) - пытается отобразить фрагмент карты (сам рисунок карты) на мастер.. (включается в опциях)

 

            наглядно видно как карта отображается на мастер. можно оценить качество. 
            хорошо бы сделать и наоборот с мастера на карту.. 

 

 

[-] файл Benyamin algo edition.vme при отключенных adaptive transitions зоны видимости отобржаются неправильно

на других файлах это не проявлялось..




28.10.2004

[+] реализация раздельной компиляции
	измения в архитектуре:
	* новый класс - CVisMap - вся работа с битовой картой видимости (кроме CVVMReal::ProcessBitMap) перенесена туда 
	(новый файл CVVMReal2.cpp)
	* массив флагов изменения точек на каждом мастере bool * CVVMReal::points_changed, размер его  [(NumOfMasters+1)*NumOfPoints]
		функции для работы с ним: CVVMReal::AllChangedPoints(bool)
		в SetPointList - сравнение нового и старого массивов точек, обнуление в CVVMReal::reset_all
	* флаг необходимости перекомпилирования битовой карты видимости мастера bool CmasterInfo::viszone_changed
	* проверка этого флага в CVVMReal::Calculatevisibility()
	* перестроен CVVMReal::CompileTrans. каждое преобразование обрабатывается единожды
	одно не изменилось - если пользователь прерывает компиляцию - система приходит в первоначальное состояние

	ect..
